<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/brand_cream"
    android:padding="32dp">

    <TextView
        android:id="@+id/emergencyTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/emergency_call_title"
        android:textSize="28sp"
        android:textColor="@color/emergency_red"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/emergencyCallButton"
        app:layout_constraintVertical_chainStyle="packed"
        android:layout_marginBottom="40dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/emergencyCallButton"
        android:layout_width="0dp"
        android:layout_height="80dp"
        android:text="@string/emergency_call_confirm"
        android:textSize="24sp"
        android:textColor="@color/white"
        android:backgroundTint="@color/emergency_red"
        app:cornerRadius="16dp"
        app:layout_constraintTop_toBottomOf="@id/emergencyTitle"
        app:layout_constraintBottom_toTopOf="@id/emergencyCancelButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="16dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/emergencyCancelButton"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:text="@string/emergency_call_cancel"
        android:textSize="18sp"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        app:strokeColor="@color/brand_brown"
        android:textColor="@color/brand_brown"
        app:cornerRadius="16dp"
        app:layout_constraintTop_toBottomOf="@id/emergencyCallButton"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
