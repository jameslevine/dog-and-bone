# Exclude from Netlify builds (reduces repo clone size and build time)

# Android launcher (internal use only, not needed for web deployment)
android/

# Development files
coverage/
.claude/

# Documentation (not needed for build)
docs/
userInstructions/

# Test files
**/*.test.ts
**/*.test.tsx

# Local environment
.env
.env.local
